# MCU Configuration
ARCH = cortex-m0
MCU = stm32f072cb

# Toolchain (adjust if your toolchain prefix is different)
TOOLCHAIN_PREFIX = arm-none-eabi-

# Project Files
PROJECT = pd_sniffer_v1
CFILES = fusb302_sniffer.c
# Header files are included, not compiled directly, but listed for dependency tracking
HFILES = fusb302.h 

# Source directories for libopencm3
LIBOPENCM3_DIR = ./libopencm3
include $(LIBOPENCM3_DIR)/mk/genlink-config.mk

# Compilation Flags
CFLAGS += -I.
CFLAGS += -std=c99
CFLAGS += -Wall -Wextra
CFLAGS += -Os
CFLAGS += -ffunction-sections -fdata-sections
CFLAGS += -DSTM32F0

# Linker Flags
LDFLAGS += -nostdlib
LDFLAGS += --gc-sections

# Include the main libopencm3 makefile which handles the build process
include $(LIBOPENCM3_DIR)/mk/Makefile.include